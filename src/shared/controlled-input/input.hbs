<input id="{{inputId}}"
       class="input-field__input input_theme_{{inputStatus}}"
       type="{{inputType}}"
    {{#if inputValue}}
       value="{{inputValue}}"
    {{/if}}
       name="{{inputName}}"
>
